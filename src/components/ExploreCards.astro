---
import { homeContent } from '../data/home';
import OptimizedImage from './OptimizedImage.astro';

const { highlights } = homeContent;
---
<section class="section-padding bg-zinc border-b border-border-subtle">
  <div class="container-luxury">
    <div class="text-center mb-8 md:mb-14 reveal-on-scroll">
      <span class="eyebrow">Explore</span>
      <h2 class="text-2xl md:text-4xl font-serif font-bold text-text-white">Everything We Offer</h2>
      <div class="gold-divider mt-4 md:mt-6"></div>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-3 gap-4 md:gap-6">
      {highlights.map((item, i) => (
        <a
          href={item.href}
          class:list={[
            'group card-surface overflow-hidden transition-all duration-500 hover:-translate-y-1 reveal-on-scroll',
            i === 1 && 'stagger-1',
            i === 2 && 'stagger-2',
          ]}
        >
          <div class="h-36 md:h-48 overflow-hidden relative">
            <OptimizedImage
              src={item.image}
              alt={item.title}
              cover
              class="transition-transform duration-700 group-hover:scale-105 opacity-90 group-hover:opacity-100"
              sizes="(max-width: 768px) 100vw, 33vw"
            />
            <div class="absolute inset-0 bg-gradient-to-t from-zinc to-transparent"></div>
          </div>
          <div class="p-5 md:p-6">
            <h3 class="text-xl font-serif font-semibold text-text-white mb-2 group-hover:text-accent-gold transition-colors duration-300">
              {item.title}
            </h3>
            <p class="text-sm text-text-ash leading-relaxed">{item.description}</p>
            <span class="inline-flex items-center gap-1 mt-4 text-xs font-semibold text-accent-gold uppercase tracking-wider min-h-11">
              Learn more
              <svg class="w-3.5 h-3.5 transition-transform group-hover:translate-x-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
              </svg>
            </span>
          </div>
        </a>
      ))}
    </div>
  </div>
</section>
